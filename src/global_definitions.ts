// @ts-check
"use strict"

export enum ServerStatus {
    Disconnected = "disconnected",
    Connecting = "connecting",
    Connected = "connected",
}

export enum DeviceStatus {
    NA = "unkown",
    Disconnected = "disconnected",
    Connecting = "connecting",
    Connected = "connected",
}
